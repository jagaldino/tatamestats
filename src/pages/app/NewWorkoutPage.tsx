import { SectionCard } from "../../components/SectionCard";

export function NewWorkoutPage() {
  return (
    <SectionCard title="/app/treinos/novo" className="bg-zinc-900/90">
      <p>
        Formulário de novo treino. Este é o ponto onde entram os MUST-02 e
        MUST-03, com entrada rápida, peso, parceiros e finalizações.
      </p>
    </SectionCard>
  );
}
